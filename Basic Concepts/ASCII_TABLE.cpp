#include <iostream>
using namespace std;

int main()
{
    // ASCII -> American Standard Code for Information Interchange

    // (int)'a' -> casting operator
    cout << (int)'a' << endl;
    cout << int('a') << endl;
    cout << int('A') << endl;

    // many standard for representating characters
    // ASCII
    cout << char(65) << endl;
}